import { PlaylistApplication } from './playlist-application';

describe('PlaylistApplication', () => {
  it('should create an instance', () => {
    expect(new PlaylistApplication()).toBeTruthy();
  });
});
